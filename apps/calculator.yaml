apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: calculator
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    path: manifests/calculator
    repoURL: git@github.com:k5sha/k5sha-gitops.git 
    targetRevision: HEAD
  destination:
    name     : in-cluster
    namespace: calculator 
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true