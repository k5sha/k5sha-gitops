apiVersion: v1
kind: ServiceAccount
metadata:
  name: tailscale-eso-sa
  namespace: tailscale
---
apiVersion:  external-secrets.io/v1alpha1
kind: SecretStore
metadata:
  name: vault-backend
  namespace: tailscale
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "eso-role" 
          serviceAccountRef:
            name: "tailscale-eso-sa"
            audiences:
              - vault