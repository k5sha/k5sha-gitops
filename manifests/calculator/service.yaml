apiVersion: v1
kind: Service
metadata:
  name: calculator-service
  namespace: calculator
  labels:            
    app: calculator
spec:
  type: ClusterIP    
  selector:
    app: calculator 
  ports:
    - name: http     
      protocol: TCP
      port: 80       
      targetPort: 8080 